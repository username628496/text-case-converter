---
phase: 06-component-fixes-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/how-it-works-section.tsx
  - src/app/[locale]/page.tsx
  - messages/en.json
  - messages/vi.json
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "A 'How it works' section appears below the tool card on the homepage"
    - "The section contains a 2-column grid of 7 mode cards"
    - "Each card shows a colored badge, mode name, 2-sentence description, and monospace before/after example"
    - "All content is translated in both en.json and vi.json with proper Vietnamese diacritics"
    - "Badge colors match the mode tab colors from CASE_MODES"
  artifacts:
    - path: "src/components/how-it-works-section.tsx"
      provides: "How it works section with 7 mode cards"
      contains: "CASE_MODES"
    - path: "messages/en.json"
      provides: "English content for 7 mode descriptions"
      contains: "howItWorks"
    - path: "messages/vi.json"
      provides: "Vietnamese content for 7 mode descriptions"
      contains: "howItWorks"
  key_links:
    - from: "src/components/how-it-works-section.tsx"
      to: "src/lib/case-transforms.ts"
      via: "import { CASE_MODES }"
      pattern: "CASE_MODES"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/how-it-works-section.tsx"
      via: "import { HowItWorksSection }"
      pattern: "HowItWorksSection"
---

<objective>
Create a "How it works" section for the homepage with 7 mode cards showing badge, name, SEO-optimized description, and before/after examples. Add full EN and VI translations.

Purpose: Make the tool self-explanatory to new visitors and boost SEO with keyword-rich mode descriptions in both languages.
Output: New HowItWorksSection component, updated homepage composition, and i18n content for 7 modes in both locales.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-component-fixes-content/06-CONTEXT.md

<interfaces>
From src/lib/case-transforms.ts:
```typescript
export type CaseMode = 'sentence' | 'lower' | 'upper' | 'capital' | 'alternating' | 'title' | 'inverse'

export const CASE_MODES = [
  { id: 'sentence' as const, abbr: 'Sc', label: 'Sentence case', color: '#ea580c' },
  { id: 'lower' as const, abbr: 'lc', label: 'lower case', color: '#16a34a' },
  { id: 'upper' as const, abbr: 'UC', label: 'UPPER CASE', color: '#2563eb' },
  { id: 'capital' as const, abbr: 'Cc', label: 'Capitalized Case', color: '#9333ea' },
  { id: 'alternating' as const, abbr: 'aC', label: 'aLtErNaTiNg', color: '#ca8a04' },
  { id: 'title' as const, abbr: 'TC', label: 'Title Case', color: '#0891b2' },
  { id: 'inverse' as const, abbr: 'iC', label: 'iNVERSE CaSe', color: '#db2777' },
] as const
```

From src/components/ui/badge.tsx: Badge component with 4px radius
From src/components/ui/card.tsx: Card component with 6px radius

From src/app/[locale]/page.tsx (current homepage composition order):
```
<ToolPage />
<ToolCards />
<FAQSection />
<RelatedTools />
<JsonLd />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n content for 7 mode descriptions and examples in EN and VI</name>
  <files>
    messages/en.json,
    messages/vi.json
  </files>
  <read_first>
    messages/en.json,
    messages/vi.json,
    src/lib/case-transforms.ts
  </read_first>
  <action>
Add a new top-level `"howItWorks"` namespace to both `messages/en.json` and `messages/vi.json`.

The input text for all before/after examples is: `tHe QuIcK bRoWn FoX`

**In messages/en.json**, add after the `"howto"` section (before `"nav"`):

```json
"howItWorks": {
  "heading": "How It Works",
  "subheading": "See how each text case mode transforms your text",
  "modes": {
    "sentence": {
      "name": "Sentence case",
      "description": "Capitalize the first letter of each sentence while converting everything else to lowercase. Perfect for cleaning up messy text into readable paragraphs.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "The quick brown fox"
    },
    "lower": {
      "name": "lower case",
      "description": "Convert all text to lowercase letters instantly. Ideal for normalizing data, creating CSS class names, or preparing text for case-sensitive comparisons.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "the quick brown fox"
    },
    "upper": {
      "name": "UPPER CASE",
      "description": "Transform your entire text to uppercase letters online. Commonly used for headings, acronyms, emphasis, or formatting text for official documents.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "THE QUICK BROWN FOX"
    },
    "capital": {
      "name": "Capitalized Case",
      "description": "Capitalize the first letter of every word in your text. Great for formatting names, titles, addresses, or any text where each word should start with a capital letter.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "The Quick Brown Fox"
    },
    "alternating": {
      "name": "aLtErNaTiNg CaSe",
      "description": "Switch each character between lowercase and uppercase, starting with lowercase. Popular for memes, sarcastic text, and playful social media posts.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "tHe qUiCk bRoWn fOx"
    },
    "title": {
      "name": "Title Case",
      "description": "Apply professional title capitalization following AP/Chicago style rules. Short words like 'a', 'the', 'of' stay lowercase unless they start the title.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "The Quick Brown Fox"
    },
    "inverse": {
      "name": "iNVERSE CaSe",
      "description": "Flip the case of every letter \u2014 uppercase becomes lowercase and vice versa. Useful for creative text effects and reversing accidental caps lock typing.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "ThE qUiCk BrOwN fOx"
    }
  }
}
```

**In messages/vi.json**, add the same `"howItWorks"` namespace with idiomatic Vietnamese (NOT word-for-word translation):

```json
"howItWorks": {
  "heading": "C\u00e1ch ho\u1ea1t \u0111\u1ed9ng",
  "subheading": "Xem c\u00e1ch m\u1ed7i ch\u1ebf \u0111\u1ed9 chuy\u1ec3n \u0111\u1ed5i v\u0103n b\u1ea3n c\u1ee7a b\u1ea1n",
  "modes": {
    "sentence": {
      "name": "Ch\u1eef c\u00e2u",
      "description": "Vi\u1ebft hoa ch\u1eef c\u00e1i \u0111\u1ea7u m\u1ed7i c\u00e2u, ph\u1ea7n c\u00f2n l\u1ea1i chuy\u1ec3n sang ch\u1eef th\u01b0\u1eddng. Ph\u00f9 h\u1ee3p \u0111\u1ec3 d\u1ecdn d\u1eb9p v\u0103n b\u1ea3n l\u1ed9n x\u1ed9n th\u00e0nh \u0111o\u1ea1n v\u0103n d\u1ec5 \u0111\u1ecdc.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "The quick brown fox"
    },
    "lower": {
      "name": "ch\u1eef th\u01b0\u1eddng",
      "description": "Chuy\u1ec3n to\u00e0n b\u1ed9 v\u0103n b\u1ea3n sang ch\u1eef th\u01b0\u1eddng ngay l\u1eadp t\u1ee9c. L\u00fd t\u01b0\u1edfng \u0111\u1ec3 chu\u1ea9n h\u00f3a d\u1eef li\u1ec7u, t\u1ea1o t\u00ean class CSS, ho\u1eb7c chu\u1ea9n b\u1ecb v\u0103n b\u1ea3n cho so s\u00e1nh ph\u00e2n bi\u1ec7t hoa th\u01b0\u1eddng.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "the quick brown fox"
    },
    "upper": {
      "name": "CH\u1eee HOA",
      "description": "Chuy\u1ec3n to\u00e0n b\u1ed9 v\u0103n b\u1ea3n sang ch\u1eef hoa tr\u1ef1c tuy\u1ebfn. Th\u01b0\u1eddng d\u00f9ng cho ti\u00eau \u0111\u1ec1, t\u1eeb vi\u1ebft t\u1eaft, nh\u1ea5n m\u1ea1nh, ho\u1eb7c \u0111\u1ecbnh d\u1ea1ng v\u0103n b\u1ea3n ch\u00ednh th\u1ee9c.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "THE QUICK BROWN FOX"
    },
    "capital": {
      "name": "Ch\u1eef Hoa \u0110\u1ea7u",
      "description": "Vi\u1ebft hoa ch\u1eef c\u00e1i \u0111\u1ea7u c\u1ee7a m\u1ecdi t\u1eeb trong v\u0103n b\u1ea3n. Tuy\u1ec7t v\u1eddi \u0111\u1ec3 \u0111\u1ecbnh d\u1ea1ng t\u00ean ri\u00eang, ti\u00eau \u0111\u1ec1, \u0111\u1ecba ch\u1ec9, ho\u1eb7c b\u1ea5t k\u1ef3 v\u0103n b\u1ea3n n\u00e0o c\u1ea7n vi\u1ebft hoa \u0111\u1ea7u t\u1eeb.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "The Quick Brown Fox"
    },
    "alternating": {
      "name": "xEn k\u1ebd",
      "description": "Xen k\u1ebd gi\u1eefa ch\u1eef th\u01b0\u1eddng v\u00e0 ch\u1eef hoa cho t\u1eebng k\u00fd t\u1ef1, b\u1eaft \u0111\u1ea7u b\u1eb1ng ch\u1eef th\u01b0\u1eddng. Ph\u1ed5 bi\u1ebfn cho meme, v\u0103n b\u1ea3n m\u1ec9a mai, v\u00e0 b\u00e0i \u0111\u0103ng m\u1ea1ng x\u00e3 h\u1ed9i vui nh\u1ed9n.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "tHe qUiCk bRoWn fOx"
    },
    "title": {
      "name": "Ti\u00eau \u0110\u1ec1",
      "description": "\u00c1p d\u1ee5ng quy t\u1eafc vi\u1ebft hoa ti\u00eau \u0111\u1ec1 chuy\u00ean nghi\u1ec7p theo chu\u1ea9n AP/Chicago. C\u00e1c t\u1eeb ng\u1eafn nh\u01b0 'a', 'the', 'of' gi\u1eef nguy\u00ean ch\u1eef th\u01b0\u1eddng tr\u1eeb khi \u1edf \u0111\u1ea7u ti\u00eau \u0111\u1ec1.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "The Quick Brown Fox"
    },
    "inverse": {
      "name": "\u0110\u1ea2O nG\u01af\u1ee2C",
      "description": "L\u1eadt ng\u01b0\u1ee3c ki\u1ec3u ch\u1eef c\u1ee7a m\u1ecdi k\u00fd t\u1ef1 \u2014 ch\u1eef hoa th\u00e0nh ch\u1eef th\u01b0\u1eddng v\u00e0 ng\u01b0\u1ee3c l\u1ea1i. H\u1eefu \u00edch cho hi\u1ec7u \u1ee9ng v\u0103n b\u1ea3n s\u00e1ng t\u1ea1o v\u00e0 s\u1eeda l\u1ed7i g\u00f5 nh\u1ea7m Caps Lock.",
      "before": "tHe QuIcK bRoWn FoX",
      "after": "ThE qUiCk BrOwN fOx"
    }
  }
}
```

Place the `"howItWorks"` key after `"howto"` and before `"nav"` in both files to keep a logical ordering.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && node -e "const en=require('./messages/en.json'); const vi=require('./messages/vi.json'); const modes=['sentence','lower','upper','capital','alternating','title','inverse']; let ok=true; modes.forEach(m=>{if(!en.howItWorks?.modes?.[m]?.description){console.log('EN missing:',m);ok=false}; if(!vi.howItWorks?.modes?.[m]?.description){console.log('VI missing:',m);ok=false}}); if(ok) console.log('All 7 modes present in both locales')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "howItWorks" messages/en.json` returns a match
    - `grep "howItWorks" messages/vi.json` returns a match
    - en.json contains `"howItWorks"` key with `"heading"`, `"subheading"`, and `"modes"` containing all 7 mode IDs (sentence, lower, upper, capital, alternating, title, inverse)
    - vi.json contains matching `"howItWorks"` structure with all 7 modes
    - Each mode entry has `"name"`, `"description"`, `"before"`, `"after"` fields
    - All "before" values are `"tHe QuIcK bRoWn FoX"`
    - en.json and vi.json remain valid JSON (no trailing commas, no syntax errors)
  </acceptance_criteria>
  <done>Both en.json and vi.json contain howItWorks namespace with 7 mode cards including descriptions and before/after examples. Vietnamese content uses idiomatic phrasing with proper diacritics.</done>
</task>

<task type="auto">
  <name>Task 2: Create HowItWorksSection component and wire into homepage</name>
  <files>
    src/components/how-it-works-section.tsx,
    src/app/[locale]/page.tsx
  </files>
  <read_first>
    src/app/[locale]/page.tsx,
    src/lib/case-transforms.ts,
    src/components/ui/badge.tsx,
    src/components/ui/card.tsx,
    messages/en.json
  </read_first>
  <action>
**Create `src/components/how-it-works-section.tsx`:**

This is a Server Component (no 'use client'). It receives translations as props from the page.

```tsx
import { CASE_MODES } from '@/lib/case-transforms'
import { Badge } from '@/components/ui/badge'

interface ModeContent {
  name: string
  description: string
  before: string
  after: string
}

interface HowItWorksSectionProps {
  heading: string
  subheading: string
  modes: Record<string, ModeContent>
}

export function HowItWorksSection({ heading, subheading, modes }: HowItWorksSectionProps) {
  return (
    <section className="mt-10">
      <h2 className="text-xl font-semibold text-[var(--color-navy)] dark:text-zinc-200 mb-1">
        {heading}
      </h2>
      <p className="text-[15px] text-zinc-500 dark:text-zinc-400 mb-6">
        {subheading}
      </p>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {CASE_MODES.map((mode) => {
          const content = modes[mode.id]
          if (!content) return null
          return (
            <div
              key={mode.id}
              className="rounded-[6px] p-4 bg-[#f0faf8] dark:bg-[#1a2f28] border border-[var(--color-border-brand)]"
            >
              <div className="flex items-center gap-2 mb-2">
                <Badge
                  style={{ backgroundColor: mode.color }}
                  className="text-white text-xs w-6 h-6 justify-center p-0 border-transparent"
                >
                  {mode.abbr}
                </Badge>
                <span className="text-[15px] font-medium text-[var(--color-navy)] dark:text-zinc-100">
                  {content.name}
                </span>
              </div>
              <p className="text-sm text-zinc-600 dark:text-zinc-400 mb-3">
                {content.description}
              </p>
              <div className="font-mono text-[13px] bg-white dark:bg-zinc-900 rounded-[4px] p-2 border border-zinc-200 dark:border-zinc-700">
                <div className="text-zinc-400">{content.before}</div>
                <div className="text-[var(--color-navy)] dark:text-zinc-100 font-semibold">{content.after}</div>
              </div>
            </div>
          )
        })}
      </div>
    </section>
  )
}
```

Key design decisions:
- Mint tint background `#f0faf8` (light) / `#1a2f28` (dark) per CONTEXT.md
- Badge colors driven by `CASE_MODES[n].color` for consistency with tabs
- Before/after in monospace with `font-mono text-[13px]` on white background
- 7th card sits at half-width, left-aligned (no span treatment) per CONTEXT.md
- No icon in section heading (plain h2) per CONTEXT.md -- HelpCircle is FAQ only

**Update `src/app/[locale]/page.tsx`:**

1. Add import at top:
```tsx
import { HowItWorksSection } from '@/components/how-it-works-section'
```

2. Add translations loading. A `tHowItWorks` translator is needed. Add after the existing `const tHowto = ...` line:
```tsx
const tHowItWorks = await getTranslations({ locale, namespace: 'howItWorks' })
```

3. Extract the modes data using `tHowItWorks.raw('modes')`:
```tsx
const howItWorksModes = tHowItWorks.raw('modes') as Record<string, { name: string; description: string; before: string; after: string }>
```

4. Insert `<HowItWorksSection>` between `<ToolPage />` and `<ToolCards>` in the JSX:
```tsx
<ToolPage />

<HowItWorksSection
  heading={tHowItWorks('heading')}
  subheading={tHowItWorks('subheading')}
  modes={howItWorksModes}
/>

<ToolCards locale={locale} toolNames={toolNames} />
```

This places the "How it works" section below the tool card and above the ToolCards grid, per CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && grep "HowItWorksSection" src/app/[locale]/page.tsx && grep "CASE_MODES" src/components/how-it-works-section.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/how-it-works-section.tsx` exists
    - `grep "import { CASE_MODES }" src/components/how-it-works-section.tsx` returns a match
    - `grep "import { HowItWorksSection }" src/app/[locale]/page.tsx` returns a match
    - `grep "tHowItWorks" src/app/[locale]/page.tsx` returns at least 3 matches (declaration + heading + subheading + modes)
    - `grep "#f0faf8" src/components/how-it-works-section.tsx` returns a match (mint tint bg)
    - `grep "font-mono" src/components/how-it-works-section.tsx` returns a match (monospace before/after)
    - `grep "grid-cols-2" src/components/how-it-works-section.tsx` returns a match (2-column grid)
    - In page.tsx, `<HowItWorksSection` appears before `<ToolCards` in the JSX
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>HowItWorksSection component renders a 2-column grid of 7 mode cards with colored badges, descriptions, and monospace before/after examples. Inserted on homepage between ToolPage and ToolCards. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. Homepage (/ and /vi/) shows "How it works" section with 7 mode cards below the converter tool
3. Each card shows correct badge color matching its tab, mode name, description, and before/after example
4. Vietnamese page shows idiomatic VI translations with proper diacritics
5. 7th card (iNVERSE CaSe) sits at half-width in left column on desktop
</verification>

<success_criteria>
- HowItWorksSection component exists and is wired into homepage
- 7 mode cards render in 2-column grid with badges, descriptions, and examples
- en.json and vi.json both contain howItWorks namespace with all 7 modes
- Section appears between ToolPage and ToolCards on the homepage
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-component-fixes-content/06-02-SUMMARY.md`
</output>
