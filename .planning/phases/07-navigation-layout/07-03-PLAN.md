---
phase: 07-navigation-layout
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/components/sidebar-nav.tsx
  - src/app/[locale]/layout.tsx
  - src/components/site-nav.tsx
  - messages/en.json
  - messages/vi.json
autonomous: true
requirements: [NAV-03, NAV-04]

must_haves:
  truths:
    - "The desktop sidebar shows grouped tool navigation links (Text Tools, Code & Data, Random categories) below the ad slot"
    - "Each sidebar link uses a locale-prefixed URL and highlights the current page with navy background + white text"
    - "The mobile Sheet drawer shows the same tool groups with actual links (not 'Coming soon' placeholders)"
    - "Sidebar nav is accessible from the mobile hamburger menu on narrow viewports"
    - "npm run build succeeds with 0 errors"
  artifacts:
    - path: "src/components/sidebar-nav.tsx"
      provides: "SidebarNav Server Component with locale-prefixed links and active-page styling"
      exports: ["SidebarNav", "TOOL_GROUPS"]
    - path: "src/app/[locale]/layout.tsx"
      provides: "Layout with SidebarNav placed after sidebar ad slot"
      contains: "SidebarNav"
    - path: "src/components/site-nav.tsx"
      provides: "Mobile Sheet drawer with real tool links replacing Coming soon placeholders"
      contains: "SidebarNav"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/sidebar-nav.tsx"
      via: "import + JSX in aside element after ad slot"
      pattern: "import.*SidebarNav"
    - from: "src/components/site-nav.tsx"
      to: "src/components/sidebar-nav.tsx"
      via: "import SidebarNavLinks + render inside Sheet"
      pattern: "import.*SidebarNav"
    - from: "src/components/sidebar-nav.tsx"
      to: "src/lib/tools.ts"
      via: "import tools array for href construction"
      pattern: "import.*tools"
---

<objective>
Create the SidebarNav Server Component with grouped tool links and locale-prefixed URLs, wire it into the desktop sidebar in layout.tsx, and replace the "Coming soon" placeholders in the mobile Sheet drawer in site-nav.tsx.

Purpose: NAV-03 requires grouped tool navigation in the sidebar with active page styling. NAV-04 requires those links to be accessible in the mobile Sheet drawer. This plan depends on plan 02 being complete because both touch layout.tsx — this plan adds SidebarNav after the footer added in plan 02.

Output: src/components/sidebar-nav.tsx + updated layout.tsx sidebar aside + updated site-nav.tsx mobile drawer.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-navigation-layout/07-RESEARCH.md
@.planning/phases/07-navigation-layout/07-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from the existing codebase. Executor uses these directly. -->

From src/lib/tools.ts (tool slug + category structure):
```typescript
export interface Tool {
  slug: string
  i18nKey: string       // e.g. 'tools.caseConverter'
  category: string      // 'text' | 'encoding' | 'generator'
  isHomepage?: boolean
}
export const tools: Tool[] = [
  { slug: 'case-converter', i18nKey: 'tools.caseConverter', category: 'text', isHomepage: true },
  { slug: 'reverse-text', i18nKey: 'tools.reverseText', category: 'text' },
  { slug: 'base64-encode-decode', i18nKey: 'tools.base64', category: 'encoding' },
  { slug: 'slug-generator', i18nKey: 'tools.slugGenerator', category: 'text' },
  { slug: 'password-generator', i18nKey: 'tools.passwordGenerator', category: 'generator' },
]
```

Locale-aware href pattern (from src/components/tool-cards.tsx):
```typescript
const href = tool.isHomepage
  ? locale === 'en' ? '/' : '/vi/'
  : locale === 'en' ? `/${tool.slug}/` : `/vi/${tool.slug}/`
```

From src/app/[locale]/layout.tsx — current sidebar aside (after plan 02):
```tsx
<aside className="hidden lg:block w-72 shrink-0">
  <div
    className="min-h-[250px] w-full bg-zinc-50 dark:bg-zinc-900 flex items-center justify-center text-xs text-zinc-400 sticky top-6"
    aria-hidden="true"
    data-ad-slot="sidebar"
  >
    {tLayout('adPlaceholder')}
  </div>
</aside>
```
SidebarNav goes AFTER the data-ad-slot="sidebar" div, INSIDE the aside.

From src/components/site-nav.tsx — current mobile Sheet drawer (to be updated):
```tsx
<SheetContent side="left" className="w-72">
  <SheetHeader>
    <SheetTitle>{siteName}</SheetTitle>
  </SheetHeader>
  <nav className="mt-6 space-y-6">
    <div>
      <h3 className="text-xs font-semibold text-zinc-400 uppercase tracking-wider">
        {translations.textTools}
      </h3>
      <p className="text-sm text-zinc-500 mt-2 italic">Coming soon</p>
    </div>
    {/* ...3 more identical Coming soon groups... */}
  </nav>
</SheetContent>
```

From src/components/site-nav.tsx — SiteNavProps interface:
```typescript
interface SiteNavProps {
  locale: string
  siteName: string
  translations: {
    textTools: string
    codeData: string
    fontStyles: string
    randomGenerators: string
    toggleDark: string
    switchLocale: string
    menu: string
  }
}
```

Dark mode + brand color pattern (from site-nav.tsx):
```typescript
className="text-[var(--color-navy)] dark:text-zinc-100"
className="bg-[var(--color-mint)]"  // for active tint
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar nav translations to en.json and vi.json</name>
  <files>
    messages/en.json
    messages/vi.json
  </files>
  <read_first>
    - messages/en.json (find the "nav" namespace to add sidebar group label keys)
    - messages/vi.json (find the "nav" namespace to mirror additions)
  </read_first>
  <action>
The "nav" namespace already exists in both files with keys: textTools, codeData, fontStyles, randomGenerators, toggleDark, switchLocale, menu.

Add two new keys to the existing `"nav"` object in BOTH files:

**In messages/en.json, inside "nav":**
```json
"sidebarTools": "Tools",
"randomGeneratorsShort": "Random"
```

These labels are used as group headings in the sidebar nav. The existing "textTools" ("Text Tools") and "codeData" ("Code & Data") keys are already present and will be reused.

**In messages/vi.json, inside "nav":**
```json
"sidebarTools": "Công cụ",
"randomGeneratorsShort": "Ngẫu nhiên"
```

Note: Do NOT add a new top-level namespace. Extend the existing "nav" object. Verify JSON remains valid after addition.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && node -e "const en = require('./messages/en.json'); const vi = require('./messages/vi.json'); console.log('EN nav keys:', Object.keys(en.nav)); console.log('VI nav.sidebarTools:', vi.nav.sidebarTools);"</automated>
  </verify>
  <done>
    - en.nav.sidebarTools === "Tools"
    - vi.nav.sidebarTools === "Công cụ" (with diacritics)
    - Both files parse as valid JSON
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SidebarNav component, wire into layout and mobile drawer</name>
  <files>
    src/components/sidebar-nav.tsx
    src/app/[locale]/layout.tsx
    src/components/site-nav.tsx
  </files>
  <read_first>
    - src/app/[locale]/layout.tsx (current full content after plan 02 — understand sidebar aside structure and existing imports)
    - src/components/site-nav.tsx (current full content — understand SiteNavProps, mobile Sheet structure to replace Coming soon sections)
    - src/lib/tools.ts (tools array, Tool interface, locale-aware href pattern)
    - messages/en.json (verify nav namespace key names used in SidebarNav match actual JSON keys)
  </read_first>
  <action>
**Create src/components/sidebar-nav.tsx** as a Server Component (async function, no 'use client'):

```typescript
import Link from 'next/link'
import { getTranslations } from 'next-intl/server'
import { tools } from '@/lib/tools'

interface SidebarNavProps {
  locale: string
  currentSlug?: string  // active tool slug, e.g. 'reverse-text'; undefined on homepage
}

// Tool groups using category from tools.ts
// Text Tools: text category
// Code & Data: encoding category
// Random: generator category
const TOOL_GROUPS = [
  {
    labelKey: 'textTools',  // maps to nav.textTools in messages
    category: 'text',
  },
  {
    labelKey: 'codeData',   // maps to nav.codeData in messages
    category: 'encoding',
  },
  {
    labelKey: 'randomGenerators',  // maps to nav.randomGenerators in messages
    category: 'generator',
  },
]

export { TOOL_GROUPS }

export async function SidebarNav({ locale, currentSlug }: SidebarNavProps) {
  const tNav = await getTranslations({ locale, namespace: 'nav' })
  const tTools = await getTranslations({ locale, namespace: 'tools' })

  return (
    <nav className="mt-4 space-y-5" aria-label={tNav('sidebarTools')}>
      {TOOL_GROUPS.map(({ labelKey, category }) => {
        const groupTools = tools.filter(t => t.category === category)
        if (groupTools.length === 0) return null

        return (
          <div key={category}>
            <h3 className="text-xs font-semibold text-zinc-400 dark:text-zinc-500 uppercase tracking-wider mb-2">
              {tNav(labelKey as 'textTools' | 'codeData' | 'randomGenerators')}
            </h3>
            <ul className="space-y-0.5">
              {groupTools.map(tool => {
                const href = tool.isHomepage
                  ? locale === 'en' ? '/' : '/vi/'
                  : locale === 'en' ? `/${tool.slug}/` : `/vi/${tool.slug}/`

                const isActive = tool.isHomepage
                  ? currentSlug === undefined
                  : currentSlug === tool.slug

                const toolSubKey = tool.i18nKey.replace('tools.', '')  // e.g. 'caseConverter'
                const toolName = tTools(`${toolSubKey}.name`)

                return (
                  <li key={tool.slug}>
                    <Link
                      href={href}
                      className={`block px-2 py-1.5 rounded-[4px] text-[14px] transition-colors ${
                        isActive
                          ? 'bg-[var(--color-navy)] text-white font-medium'
                          : 'text-zinc-700 dark:text-zinc-300 hover:bg-[var(--color-mint)] dark:hover:bg-zinc-800 hover:text-[var(--color-navy)]'
                      }`}
                      aria-current={isActive ? 'page' : undefined}
                    >
                      {toolName}
                    </Link>
                  </li>
                )
              })}
            </ul>
          </div>
        )
      })}
    </nav>
  )
}
```

**In src/app/[locale]/layout.tsx:**

The layout.tsx receives `params` containing `locale` but NOT `tool` slug. The sidebar nav needs to show the active tool. For layout-level components, we cannot determine the current tool slug from layout params alone — pass `currentSlug={undefined}` from layout (no active state at layout level; active styling on tool pages is a future enhancement). The nav still renders all links correctly.

Add import:
```typescript
import { SidebarNav } from '@/components/sidebar-nav'
```

Add `getTranslations` call for nav (already present for tNav — no new call needed, tNav already fetched).

In the `<aside>` element, add SidebarNav AFTER the existing sidebar ad slot div:
```tsx
<aside className="hidden lg:block w-72 shrink-0">
  {/* Sidebar ad slot: 250px min-height — PRESERVE AS-IS */}
  <div
    className="min-h-[250px] w-full bg-zinc-50 dark:bg-zinc-900 flex items-center justify-center text-xs text-zinc-400 sticky top-6"
    aria-hidden="true"
    data-ad-slot="sidebar"
  >
    {tLayout('adPlaceholder')}
  </div>
  {/* Sidebar tool navigation — added in Phase 07 */}
  <SidebarNav locale={locale} />
</aside>
```

Do NOT remove the data-ad-slot="sidebar" div. Do NOT change its classes.

**In src/components/site-nav.tsx:**

site-nav.tsx is a Client Component ('use client'). SidebarNav is a Server Component. We cannot directly import an async Server Component into a Client Component.

Solution: Extract only the link rendering logic as a separate shared Client-compatible component, OR pass tool links as props from layout.

The cleanest approach: Pass pre-rendered tool link data as a prop to SiteNav.

Add `toolGroups` to SiteNavProps:
```typescript
interface SiteNavProps {
  locale: string
  siteName: string
  translations: {
    textTools: string
    codeData: string
    fontStyles: string
    randomGenerators: string
    toggleDark: string
    switchLocale: string
    menu: string
    sidebarTools: string  // add this
  }
  toolGroups: Array<{       // add this
    label: string
    tools: Array<{ name: string; href: string; slug: string }>
  }>
}
```

In the mobile Sheet drawer, replace each "Coming soon" section with actual link lists using `toolGroups`:
```tsx
<nav className="mt-6 space-y-6">
  {toolGroups.map(group => (
    <div key={group.label}>
      <h3 className="text-xs font-semibold text-zinc-400 uppercase tracking-wider">
        {group.label}
      </h3>
      <ul className="mt-2 space-y-1">
        {group.tools.map(tool => (
          <li key={tool.slug}>
            <a
              href={tool.href}
              className="block px-2 py-1.5 rounded-[4px] text-sm text-zinc-700 dark:text-zinc-300 hover:bg-[var(--color-mint)] dark:hover:bg-zinc-800 hover:text-[var(--color-navy)]"
            >
              {tool.name}
            </a>
          </li>
        ))}
      </ul>
    </div>
  ))}
</nav>
```

In src/app/[locale]/layout.tsx, build the toolGroups data and pass it to SiteNav. Add this data-building logic after the existing tNav calls:
```typescript
// Build tool groups for mobile nav drawer
const tTools = await getTranslations({ locale, namespace: 'tools' })

const toolGroupsData = [
  {
    label: tNav('textTools'),
    tools: tools.filter(t => t.category === 'text').map(t => {
      const subKey = t.i18nKey.replace('tools.', '')
      const href = t.isHomepage
        ? locale === 'en' ? '/' : '/vi/'
        : locale === 'en' ? `/${t.slug}/` : `/vi/${t.slug}/`
      return { name: tTools(`${subKey}.name`), href, slug: t.slug }
    }),
  },
  {
    label: tNav('codeData'),
    tools: tools.filter(t => t.category === 'encoding').map(t => {
      const subKey = t.i18nKey.replace('tools.', '')
      const href = locale === 'en' ? `/${t.slug}/` : `/vi/${t.slug}/`
      return { name: tTools(`${subKey}.name`), href, slug: t.slug }
    }),
  },
  {
    label: tNav('randomGenerators'),
    tools: tools.filter(t => t.category === 'generator').map(t => {
      const subKey = t.i18nKey.replace('tools.', '')
      const href = locale === 'en' ? `/${t.slug}/` : `/vi/${t.slug}/`
      return { name: tTools(`${subKey}.name`), href, slug: t.slug }
    }),
  },
]
```

Then add `toolGroups={toolGroupsData}` and `sidebarTools: tNav('sidebarTools')` to the SiteNav JSX call.

Also add `import { tools } from '@/lib/tools'` to layout.tsx imports.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - src/components/sidebar-nav.tsx exists and exports SidebarNav async function
    - sidebar-nav.tsx has no 'use client' directive
    - layout.tsx imports SidebarNav, imports tools from @/lib/tools, and renders &lt;SidebarNav locale={locale} /&gt; inside the aside after the ad slot div
    - site-nav.tsx SiteNavProps has toolGroups field; mobile Sheet renders tool links (not "Coming soon")
    - npm run build exits 0 with no TypeScript errors
    - grep "Coming soon" src/components/site-nav.tsx returns no matches
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:
- `npm run build` exits 0
- `grep -n "SidebarNav" src/app/\[locale\]/layout.tsx` shows import and JSX usage
- `grep "Coming soon" src/components/site-nav.tsx` returns no matches (placeholders replaced)
- `grep "aria-current" src/components/sidebar-nav.tsx` returns a match (accessibility)
- `grep "data-ad-slot=\"sidebar\"" src/app/\[locale\]/layout.tsx` still present (ad slot preserved)
</verification>

<success_criteria>
1. Desktop sidebar shows 3 tool groups (Text Tools, Code & Data, Random Generators) each with tool links using locale-prefixed hrefs
2. Active page link uses bg-[var(--color-navy)] text-white styling; inactive links use hover:bg-[var(--color-mint)] hover:text-[var(--color-navy)]
3. Mobile Sheet drawer replaces "Coming soon" text with actual tool link lists matching the desktop groups
4. Sidebar ad slot (data-ad-slot="sidebar") div is preserved unchanged
5. npm run build succeeds with 0 TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-layout/07-03-SUMMARY.md`
</output>
