---
phase: 05-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - TYPO-01
  - SURF-01
  - SURF-02
  - SURF-03

must_haves:
  truths:
    - "All text renders in Inter font (not Geist Sans)"
    - "Monospace text renders in JetBrains Mono (not Geist Mono)"
    - "Page body background is #e8f5f2 in light mode"
    - "Page body background is #0f1a17 in dark mode"
    - "Surface cards render on white (#fff) in light mode"
    - "Surface cards render on #162520 in dark mode"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Inter + JetBrains Mono font imports"
      contains: "Inter"
    - path: "src/app/globals.css"
      provides: "Mint color palette CSS variables"
      contains: "#e8f5f2"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-inter mapped to --font-sans"
      pattern: "--font-inter"
---

<objective>
Replace Geist fonts with Inter + JetBrains Mono, and update CSS variables to establish the mint/dark color palette as the foundation for all subsequent visual changes.

Purpose: This is the visual foundation — font and color tokens that every component inherits.
Output: Updated layout.tsx (font imports) and globals.css (color variables).
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-foundation/05-CONTEXT.md

<interfaces>
<!-- Current font setup in layout.tsx (lines 1-13): -->
```typescript
import { Geist, Geist_Mono } from 'next/font/google'
const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] })
const geistMono = Geist_Mono({ variable: '--font-geist-mono', subsets: ['latin'] })
// Applied on <html>: className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
```

<!-- Current CSS variable architecture in globals.css: -->
```css
/* @theme inline block maps CSS vars to Tailwind utilities */
--font-sans: var(--font-sans);
--font-mono: var(--font-geist-mono);

/* :root color tokens */
--background: oklch(1 0 0);        /* white - used by body via bg-background */
--card: oklch(1 0 0);              /* white */
--color-bg-page: #e8f5f2;          /* already defined but NOT used for body */

/* .dark color tokens */
--background: oklch(0.145 0 0);    /* near-black */
--card: oklch(0.205 0 0);          /* dark gray */
--color-bg-page: oklch(0.205 0 0); /* dark gray */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Geist fonts to Inter + JetBrains Mono in root layout</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx
    - node_modules/next/dist/docs/ (check for font import guidance if available)
  </read_first>
  <action>
Replace the font imports and configuration in src/app/layout.tsx:

1. Remove the two Geist imports:
   ```
   import { Geist, Geist_Mono } from 'next/font/google'
   ```

2. Add Inter and JetBrains Mono imports:
   ```typescript
   import { Inter, JetBrains_Mono } from 'next/font/google'
   ```

3. Replace font const declarations:
   ```typescript
   const inter = Inter({
     variable: '--font-inter',
     subsets: ['latin', 'vietnamese'],
   })

   const jetbrainsMono = JetBrains_Mono({
     variable: '--font-jetbrains-mono',
     subsets: ['latin', 'vietnamese'],
   })
   ```

4. Update the html element className:
   Change `${geistSans.variable} ${geistMono.variable}` to `${inter.variable} ${jetbrainsMono.variable}`

The rest of layout.tsx (ThemeProvider, body classes) stays identical.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && grep -c "Inter" src/app/layout.tsx && grep -c "JetBrains_Mono" src/app/layout.tsx && grep -c "Geist" src/app/layout.tsx | grep "^0$" && grep "vietnamese" src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `import { Inter, JetBrains_Mono } from 'next/font/google'`
    - src/app/layout.tsx contains `variable: '--font-inter'`
    - src/app/layout.tsx contains `variable: '--font-jetbrains-mono'`
    - src/app/layout.tsx contains `subsets: ['latin', 'vietnamese']` (twice, once per font)
    - src/app/layout.tsx does NOT contain the string `Geist` anywhere
    - src/app/layout.tsx html element uses `${inter.variable} ${jetbrainsMono.variable}`
  </acceptance_criteria>
  <done>Inter and JetBrains Mono fonts loaded with Vietnamese subsets; zero Geist references remain in layout.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Update globals.css color variables and font mappings for mint palette</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css
    - src/app/[locale]/layout.tsx (to understand bg-zinc-50 usage on ad slots)
  </read_first>
  <action>
Update src/app/globals.css with these specific changes:

1. In the `@theme inline` block, update font mappings:
   - Change `--font-sans: var(--font-sans);` to `--font-sans: var(--font-inter);`
   - Change `--font-mono: var(--font-geist-mono);` to `--font-mono: var(--font-jetbrains-mono);`

2. In the `:root` block, update color variables:
   - Change `--background: oklch(1 0 0);` to `--background: #e8f5f2;` (mint green page body)
   - Keep `--card: oklch(1 0 0);` as-is (white surfaces in light mode) -- this is correct for SURF-03
   - Keep `--popover: oklch(1 0 0);` as-is (white popover surfaces)

3. In the `.dark` block, update color variables:
   - Change `--background: oklch(0.145 0 0);` to `--background: #0f1a17;` (dark mint body)
   - Change `--card: oklch(0.205 0 0);` to `--card: #162520;` (slightly lighter dark mint surface)
   - Change `--card-foreground: oklch(0.985 0 0);` — keep as-is (white text on dark cards)
   - Change `--popover: oklch(0.205 0 0);` to `--popover: #162520;` (match card surface in dark)
   - Change `--color-bg-page: oklch(0.205 0 0);` to `--color-bg-page: #0f1a17;` (match dark body)
   - Change `--sidebar: oklch(0.205 0 0);` to `--sidebar: #162520;` (sidebar matches card surface)

4. Do NOT change any other variables (--primary, --secondary, --muted, --accent, --border, --input, --ring, charts, etc.)

5. Do NOT remove the `--color-bg-page`, `--color-border-brand`, or `--color-navy` custom properties from :root — they are used elsewhere.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && grep "font-inter" src/app/globals.css && grep "font-jetbrains-mono" src/app/globals.css && grep -c "font-geist" src/app/globals.css | grep "^0$" && grep -- "--background: #e8f5f2" src/app/globals.css && grep -- "--background: #0f1a17" src/app/globals.css && grep -- "--card: #162520" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - globals.css `@theme inline` contains `--font-sans: var(--font-inter);`
    - globals.css `@theme inline` contains `--font-mono: var(--font-jetbrains-mono);`
    - globals.css does NOT contain `font-geist` anywhere
    - globals.css `:root` contains `--background: #e8f5f2;`
    - globals.css `.dark` contains `--background: #0f1a17;`
    - globals.css `.dark` contains `--card: #162520;`
    - globals.css `.dark` contains `--popover: #162520;`
    - globals.css `.dark` contains `--sidebar: #162520;`
    - globals.css `:root` still contains `--card: oklch(1 0 0);` (white surfaces in light mode)
    - globals.css `:root` still contains `--color-bg-page: #e8f5f2;`
    - globals.css `:root` still contains `--color-navy: #1a2744;`
  </acceptance_criteria>
  <done>Body background is mint (#e8f5f2) in light mode and #0f1a17 in dark mode; cards/surfaces are white in light and #162520 in dark; fonts mapped to Inter and JetBrains Mono</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Visual inspection: page body shows mint green background (not white) in light mode
- Visual inspection: text renders in Inter (check browser DevTools computed font-family)
</verification>

<success_criteria>
- Zero Geist references in layout.tsx and globals.css
- Inter + JetBrains Mono loaded with vietnamese subsets
- Body background is #e8f5f2 (light) / #0f1a17 (dark)
- Card surfaces are white (light) / #162520 (dark)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-foundation/05-01-SUMMARY.md`
</output>
