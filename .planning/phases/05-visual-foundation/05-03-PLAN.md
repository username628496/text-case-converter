---
phase: 05-visual-foundation
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/card.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/input.tsx
  - src/components/ui/accordion.tsx
  - src/components/ui/navigation-menu.tsx
  - src/components/tool-page.tsx
  - src/components/tool-cards.tsx
  - src/components/faq-section.tsx
  - src/components/tools/base64-tool.tsx
autonomous: true
requirements:
  - SHAPE-01
  - SHAPE-02
  - SHAPE-03
  - SHAPE-04
  - SHAPE-05
  - SHAPE-06
  - SHAPE-07
  - SHAPE-08
  - SHAPE-09

must_haves:
  truths:
    - "Buttons use 4px border radius, not rounded-md (6px) or rounded-lg"
    - "Badge abbreviations use 4px radius, not rounded-full"
    - "Tool cards use 6px (rounded-md) border radius"
    - "Tab pills use 4px radius"
    - "Textarea and input use 6px radius"
    - "FAQ accordion items are square-cornered (0px radius)"
    - "Related tool chip links use 4px radius"
    - "Nav hover backgrounds use 4px radius"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button with 4px radius"
      contains: "rounded-[4px]"
    - path: "src/components/ui/badge.tsx"
      provides: "Badge with 4px radius"
      contains: "rounded-[4px]"
    - path: "src/components/ui/card.tsx"
      provides: "Card with 6px radius"
      contains: "rounded-md"
    - path: "src/components/ui/accordion.tsx"
      provides: "Accordion with 0px radius"
      contains: "rounded-none"
  key_links:
    - from: "src/components/tool-page.tsx"
      to: "src/components/ui/badge.tsx"
      via: "Badge used in case mode tabs"
      pattern: "Badge"
    - from: "src/components/tool-cards.tsx"
      to: "src/components/ui/badge.tsx"
      via: "Badge used in tool card and related tool chips"
      pattern: "Badge"
---

<objective>
Apply the 3-tier border radius system (0px for FAQ, 4px for buttons/badges/tabs/chips/nav, 6px for cards/inputs/textareas) across all UI primitives and usage sites.

Purpose: Establish rectangular visual identity — no rounded-full shapes on interactive elements.
Output: 8 UI components and 3 usage-site components updated with correct radii.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-foundation/05-CONTEXT.md

<interfaces>
<!-- Current radius classes in use: -->
<!-- button.tsx: `rounded-md` in base CVA string (line 8), `rounded-md` in size sm/lg variants -->
<!-- badge.tsx: `rounded-full` in base CVA string (line 7) -->
<!-- card.tsx: `rounded-lg` in Card className (line 12) -->
<!-- tabs.tsx: TabsList `rounded-md` (line 17), TabsTrigger `rounded-sm` (line 32) -->
<!-- textarea.tsx: `rounded-md` (line 12) -->
<!-- input.tsx: `rounded-md` (line 11) -->
<!-- accordion.tsx: no radius class (border-b only) -->
<!-- navigation-menu.tsx: `rounded-md` in trigger style (line 44), viewport (line 89) -->

<!-- Tool-page.tsx inline tab styles (lines 135-136): `rounded-md` on both active and inactive tabs -->
<!-- Tool-cards.tsx related links (line 92): `rounded-md` -->

<!-- Tailwind radius reference: -->
<!-- rounded-none = 0px, rounded-[4px] = 4px, rounded-sm = 2px, rounded-md = 6px, rounded-lg = 8px -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update border radius in all 8 shadcn UI primitive components</name>
  <files>
    src/components/ui/button.tsx
    src/components/ui/badge.tsx
    src/components/ui/card.tsx
    src/components/ui/tabs.tsx
    src/components/ui/textarea.tsx
    src/components/ui/input.tsx
    src/components/ui/accordion.tsx
    src/components/ui/navigation-menu.tsx
  </files>
  <read_first>
    - src/components/ui/button.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/card.tsx
    - src/components/ui/tabs.tsx
    - src/components/ui/textarea.tsx
    - src/components/ui/input.tsx
    - src/components/ui/accordion.tsx
    - src/components/ui/navigation-menu.tsx
  </read_first>
  <action>
**button.tsx** (SHAPE-01, SHAPE-02):
1. In the CVA base string (line 8): Change `rounded-md` to `rounded-[4px]`
2. In size.sm variant (line 24): Change `rounded-md` to `rounded-[4px]`
3. In size.lg variant (line 25): Change `rounded-md` to `rounded-[4px]`

**badge.tsx** (SHAPE-04):
1. In the CVA base string (line 7): Change `rounded-full` to `rounded-[4px]`

**card.tsx** (SHAPE-05):
1. In Card className (line 12): Keep `rounded-lg` — wait, SHAPE-05 says 6px which is `rounded-md`. Change `rounded-lg` to `rounded-md`

**tabs.tsx** (SHAPE-03):
1. TabsList (line 17): Change `rounded-md` to `rounded-[4px]`
2. TabsTrigger (line 32): Change `rounded-sm` to `rounded-[4px]`

**textarea.tsx** (SHAPE-06):
1. In textarea className (line 12): Keep `rounded-md` — this is already 6px. No change needed.

**input.tsx** (SHAPE-06):
1. In input className (line 11): Keep `rounded-md` — already 6px. No change needed.

**accordion.tsx** (SHAPE-07):
1. AccordionItem (line 17): Add `rounded-none` to the className string to ensure 0px radius.
   Change: `cn("border-b", className)` -> `cn("border-b rounded-none", className)`
   (This is defensive — AccordionItem has no explicit radius currently but adding `rounded-none` ensures no inherited radius applies.)

**navigation-menu.tsx** (SHAPE-09):
1. In `navigationMenuTriggerStyle` CVA string (line 44): Change `rounded-md` to `rounded-[4px]`
2. In NavigationMenuViewport className (line 89): Change `rounded-md` to `rounded-[4px]`
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && grep "rounded-\[4px\]" src/components/ui/button.tsx && grep "rounded-\[4px\]" src/components/ui/badge.tsx && grep "rounded-md" src/components/ui/card.tsx && grep "rounded-\[4px\]" src/components/ui/tabs.tsx && grep "rounded-none" src/components/ui/accordion.tsx && grep "rounded-\[4px\]" src/components/ui/navigation-menu.tsx && echo "--- Verify no rounded-full in badge ---" && grep -c "rounded-full" src/components/ui/badge.tsx | grep "^0$" && echo "--- Verify no rounded-lg in card ---" && grep -c "rounded-lg" src/components/ui/card.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - button.tsx CVA base contains `rounded-[4px]` (not `rounded-md`)
    - button.tsx size.sm contains `rounded-[4px]`
    - button.tsx size.lg contains `rounded-[4px]`
    - badge.tsx CVA base contains `rounded-[4px]` (not `rounded-full`)
    - card.tsx Card contains `rounded-md` (not `rounded-lg`)
    - tabs.tsx TabsList contains `rounded-[4px]` (not `rounded-md`)
    - tabs.tsx TabsTrigger contains `rounded-[4px]` (not `rounded-sm`)
    - textarea.tsx still contains `rounded-md` (6px, correct)
    - input.tsx still contains `rounded-md` (6px, correct)
    - accordion.tsx AccordionItem contains `rounded-none`
    - navigation-menu.tsx trigger style contains `rounded-[4px]`
    - navigation-menu.tsx viewport contains `rounded-[4px]`
  </acceptance_criteria>
  <done>All 8 UI primitives have correct radii: buttons/badges/tabs/nav = 4px, cards = 6px (rounded-md), textarea/input = 6px, accordion = 0px</done>
</task>

<task type="auto">
  <name>Task 2: Update border radius at usage sites (inline styles in tool-page, tool-cards, faq-section)</name>
  <files>
    src/components/tool-page.tsx
    src/components/tool-cards.tsx
    src/components/faq-section.tsx
  </files>
  <read_first>
    - src/components/tool-page.tsx
    - src/components/tool-cards.tsx
    - src/components/faq-section.tsx
  </read_first>
  <action>
These files have inline Tailwind classes (not using shadcn components) that specify border radius directly. Update them to match the 3-tier system.

**tool-page.tsx** (SHAPE-03 tabs, SHAPE-04 badges):
1. Case mode tab buttons (lines 135-136): Both active and inactive states use `rounded-md`. Change to `rounded-[4px]`.
   - Active state string: change `rounded-md text-sm font-medium` to `rounded-[4px] text-sm font-medium`
   - Inactive state string: change `rounded-md text-sm whitespace-nowrap` to `rounded-[4px] text-sm whitespace-nowrap`

2. The Badge components inside tabs inherit from badge.tsx (already fixed in Task 1). No extra change needed for badges.

**tool-cards.tsx** (SHAPE-08 related tool chips):
1. Related tool chip links (line 92): Change `rounded-md` to `rounded-[4px]`
   Result: `className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-[4px] border border-[var(--color-border-brand)] ..."`

2. The Card components used in ToolCards inherit from card.tsx (already fixed to rounded-md in Task 1). No extra change needed for cards.

**faq-section.tsx** (SHAPE-07):
1. The AccordionItem inherits from accordion.tsx (already fixed in Task 1). No additional radius changes needed in faq-section.tsx.
2. However, verify the file does NOT add any radius overrides via className. If any exist, ensure they are `rounded-none`.

**base64-tool.tsx** (SHAPE-03 tabs): Check for inline tab styles.
1. Lines 56-57: Both encode/decode tab states use `rounded-md`. Change to `rounded-[4px]`.
   - Active: `px-4 py-1.5 rounded-md text-sm` -> `px-4 py-1.5 rounded-[4px] text-sm`
   - Inactive: `px-4 py-1.5 rounded-md text-sm` -> `px-4 py-1.5 rounded-[4px] text-sm`

Note: base64-tool.tsx is listed in files_modified for Plan 02 (typography). Since Plan 02 and 03 are both wave 2, the executor must handle both plans' changes to this file. The radius changes here are orthogonal to the typography changes — they touch different parts of the className strings.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && echo "--- tool-page tabs ---" && grep "rounded-\[4px\].*text-sm" src/components/tool-page.tsx | wc -l && echo "--- tool-cards chips ---" && grep "rounded-\[4px\].*border" src/components/tool-cards.tsx && echo "--- base64 tabs ---" && grep "rounded-\[4px\]" src/components/tools/base64-tool.tsx | wc -l && echo "--- No rounded-md left in tool-page tabs ---" && grep "rounded-md.*text-sm.*whitespace-nowrap\|rounded-md.*text-sm.*font-medium.*whitespace-nowrap" src/components/tool-page.tsx | wc -l | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - tool-page.tsx active tab class contains `rounded-[4px]` (not `rounded-md`)
    - tool-page.tsx inactive tab class contains `rounded-[4px]` (not `rounded-md`)
    - tool-cards.tsx related tool chip link contains `rounded-[4px]` (not `rounded-md`)
    - base64-tool.tsx encode/decode tab buttons contain `rounded-[4px]` (not `rounded-md`)
    - faq-section.tsx has no radius overrides conflicting with accordion.tsx's `rounded-none`
  </acceptance_criteria>
  <done>All inline radius values at usage sites match the 3-tier system: tab pills = 4px, chip links = 4px, cards inherit 6px from component, FAQ items inherit 0px from component</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Visual: No rounded-full shapes visible on badges
- Visual: Cards have subtle rounded corners (6px), buttons/tabs are nearly square (4px)
- Visual: FAQ items have square corners
</verification>

<success_criteria>
- Zero `rounded-full` in badge.tsx
- Zero `rounded-lg` in card.tsx
- All buttons/tabs/badges/nav/chips use 4px (rounded-[4px])
- Cards/textarea/input use 6px (rounded-md)
- FAQ AccordionItems use 0px (rounded-none)
- No rounded-md on inline tab styles in tool-page.tsx or base64-tool.tsx
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-foundation/05-03-SUMMARY.md`
</output>
