---
phase: 05-visual-foundation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/tool-page.tsx
  - src/components/faq-section.tsx
  - src/components/tool-cards.tsx
  - src/components/site-nav.tsx
  - src/components/tools/base64-tool.tsx
  - src/components/tools/reverse-text-tool.tsx
  - src/components/tools/slug-generator-tool.tsx
  - src/components/tools/password-generator-tool.tsx
autonomous: true
requirements:
  - TYPO-02
  - TYPO-03
  - TYPO-04
  - TYPO-05
  - TYPO-06
  - TYPO-07
  - TYPO-08
  - TYPO-09

must_haves:
  truths:
    - "Body text (descriptions) renders at 15px, not 14px"
    - "Textarea content renders at 15px with font-mono"
    - "H1 headings render at 30px (text-3xl) font-bold"
    - "H2 section headings render at 20px (text-xl)"
    - "Nav links render at 14px"
    - "Tool card titles at 14px font-medium, descriptions at 13px"
    - "FAQ questions at 15px font-semibold, answers at 14px with line-height 1.7"
    - "Stats display at 13px"
  artifacts:
    - path: "src/components/tool-page.tsx"
      provides: "Homepage tool with updated typography"
      contains: "text-3xl"
    - path: "src/components/faq-section.tsx"
      provides: "FAQ with updated typography"
      contains: "text-xl"
    - path: "src/components/tool-cards.tsx"
      provides: "Tool cards with 14px/13px typography"
      contains: "text-sm font-medium"
  key_links:
    - from: "src/components/tool-page.tsx"
      to: "globals.css font-mono"
      via: "Textarea uses font-mono class which maps to JetBrains Mono"
      pattern: "font-mono"
---

<objective>
Update all typography sizes across every component to match the requirements: larger body text (15px), larger headings (H1=30px, H2=20px), and specific sizes for nav, cards, FAQ, and stats.

Purpose: Make the site more readable with larger, intentional typography.
Output: 8 component files updated with correct font sizes.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-foundation/05-CONTEXT.md

<interfaces>
<!-- Tailwind v4 size classes reference: -->
<!-- text-xs = 12px, text-sm = 14px, text-base = 16px (closest to 15px), text-lg = 18px, text-xl = 20px, text-2xl = 24px, text-3xl = 30px -->
<!-- For exact 15px: use text-[15px] (arbitrary value) -->
<!-- For exact 13px: use text-[13px] (arbitrary value) -->
<!-- For exact 14px: text-sm works (14px) -->

<!-- Current H1 pattern (tool-page.tsx line 57): -->
```html
<h1 className="text-2xl font-bold text-[var(--color-navy)] dark:text-zinc-100">
```

<!-- Current description pattern (tool-page.tsx line 60): -->
```html
<p className="text-sm text-zinc-700 dark:text-zinc-300 mt-1">
```

<!-- Current textarea (tool-page.tsx line 73): -->
```html
className="min-h-[160px] resize-y font-mono text-sm border-[var(--color-border-brand)]"
```

<!-- Current stats (tool-page.tsx line 79): -->
```html
<span className="text-xs text-zinc-400" aria-live="polite">
```

<!-- Current FAQ heading (faq-section.tsx line 18): -->
```html
<h2 className="text-lg font-semibold ...">
```

<!-- Current FAQ question (faq-section.tsx line 25): -->
```html
<AccordionTrigger className="text-sm text-zinc-700 ...">
```

<!-- Current tool card title (tool-cards.tsx line 42): -->
```html
className="text-sm font-medium text-[var(--color-navy)]..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update typography in tool-page.tsx and all 4 sub-tool components</name>
  <files>
    src/components/tool-page.tsx
    src/components/tools/base64-tool.tsx
    src/components/tools/reverse-text-tool.tsx
    src/components/tools/slug-generator-tool.tsx
    src/components/tools/password-generator-tool.tsx
  </files>
  <read_first>
    - src/components/tool-page.tsx
    - src/components/tools/base64-tool.tsx
    - src/components/tools/reverse-text-tool.tsx
    - src/components/tools/slug-generator-tool.tsx
    - src/components/tools/password-generator-tool.tsx
  </read_first>
  <action>
Apply these typography changes consistently across all 5 files:

**H1 headings** (TYPO-04): Change `text-2xl` to `text-3xl` on every h1 element.
- tool-page.tsx line 57: `text-2xl font-bold` -> `text-3xl font-bold`
- base64-tool.tsx line 38: `text-2xl font-bold` -> `text-3xl font-bold`
- reverse-text-tool.tsx line 29: `text-2xl font-bold` -> `text-3xl font-bold`
- slug-generator-tool.tsx line 28: `text-2xl font-bold` -> `text-3xl font-bold`
- password-generator-tool.tsx line 49: `text-2xl font-bold` -> `text-3xl font-bold`

**Description text** (TYPO-02): Change `text-sm` to `text-[15px]` on the description paragraph below each h1.
- tool-page.tsx line 60: `text-sm text-zinc-700` -> `text-[15px] text-zinc-700`
- base64-tool.tsx line 41: `text-sm text-zinc-700` -> `text-[15px] text-zinc-700`
- reverse-text-tool.tsx line 32: `text-sm text-zinc-700` -> `text-[15px] text-zinc-700`
- slug-generator-tool.tsx line 31: `text-sm text-zinc-700` -> `text-[15px] text-zinc-700`
- password-generator-tool.tsx line 52: `text-sm text-zinc-700` -> `text-[15px] text-zinc-700`

**Textarea content** (TYPO-03): Change `text-sm` to `text-[15px]` on textarea elements (keep `font-mono`).
- tool-page.tsx line 73: `font-mono text-sm` -> `font-mono text-[15px]`
- base64-tool.tsx line 69: `font-mono text-sm` -> `font-mono text-[15px]`
- reverse-text-tool.tsx line 41: `font-mono text-sm` -> `font-mono text-[15px]`
- slug-generator-tool.tsx line 40: `font-mono text-sm` -> `font-mono text-[15px]`

**Output/result containers** (TYPO-03 applies to mono content too): Change `text-sm` to `text-[15px]` on output display divs that use `font-mono`.
- base64-tool.tsx line 73: output div `font-mono text-sm` -> `font-mono text-[15px]`
- reverse-text-tool.tsx line 45: output div `font-mono text-sm` -> `font-mono text-[15px]`
- slug-generator-tool.tsx line 44: output div `font-mono text-sm` -> `font-mono text-[15px]`

**Stats display** (TYPO-09): Change `text-xs` to `text-[13px]` on stats spans.
- tool-page.tsx line 79: `text-xs text-zinc-400` -> `text-[13px] text-zinc-400`
- base64-tool.tsx line 85: `text-xs text-zinc-400` -> `text-[13px] text-zinc-400`
- reverse-text-tool.tsx line 50: `text-xs text-zinc-400` -> `text-[13px] text-zinc-400`
- slug-generator-tool.tsx line 50: `text-xs text-zinc-400` -> `text-[13px] text-zinc-400`
- password-generator-tool.tsx line 64: `text-xs text-zinc-400` -> `text-[13px] text-zinc-400`

**Do NOT change** button text sizes, badge text sizes, or tab label sizes — those stay at their current values.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && grep -c "text-2xl" src/components/tool-page.tsx src/components/tools/*.tsx | grep -v ":0$" | wc -l | grep "^0$" && grep "text-3xl font-bold" src/components/tool-page.tsx && grep "text-\[15px\]" src/components/tool-page.tsx | head -3 && grep "text-\[13px\]" src/components/tool-page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `text-2xl` in tool-page.tsx and all 4 sub-tool files
    - All 5 files contain `text-3xl font-bold` on the h1 element
    - tool-page.tsx description paragraph contains `text-[15px]` (not `text-sm`)
    - tool-page.tsx textarea contains `font-mono text-[15px]`
    - tool-page.tsx stats span contains `text-[13px]`
    - Same patterns applied in base64-tool.tsx, reverse-text-tool.tsx, slug-generator-tool.tsx, password-generator-tool.tsx
  </acceptance_criteria>
  <done>All H1s are 30px, descriptions are 15px, textareas are 15px font-mono, stats are 13px across all 5 tool components</done>
</task>

<task type="auto">
  <name>Task 2: Update typography in faq-section, tool-cards, and site-nav</name>
  <files>
    src/components/faq-section.tsx
    src/components/tool-cards.tsx
    src/components/site-nav.tsx
  </files>
  <read_first>
    - src/components/faq-section.tsx
    - src/components/tool-cards.tsx
    - src/components/site-nav.tsx
  </read_first>
  <action>
**FAQ section** (TYPO-05 + TYPO-08) in faq-section.tsx:

1. H2 heading (line 18): Change `text-lg font-semibold` to `text-xl font-semibold`
   Result: `className="text-xl font-semibold text-[var(--color-navy)] dark:text-zinc-200 mb-4"`

2. AccordionTrigger (line 25): Change `text-sm` to `text-[15px] font-semibold`
   Result: `className="text-[15px] font-semibold text-zinc-700 dark:text-zinc-300"`

3. AccordionContent (line 28): Change `text-sm` to `text-sm leading-[1.7]`
   (text-sm = 14px for answers, add explicit leading-[1.7] for line-height)
   Result: `className="text-sm leading-[1.7] text-zinc-500 dark:text-zinc-400"`

**Tool cards** (TYPO-05 + TYPO-07) in tool-cards.tsx:

1. RelatedTools H2 heading (line 81): Change `text-lg font-semibold` to `text-xl font-semibold`
   Result: `className="text-xl font-semibold text-[var(--color-navy)] dark:text-zinc-200 mb-3"`

2. Tool card title link (line 42): Keep as `text-sm font-medium` — this is already 14px font-medium per TYPO-07. Correct.

3. Tool card description (line 47): Keep as `text-xs` — this is 12px. Change to `text-[13px]` per TYPO-07.
   Result: `className="text-[13px] text-zinc-500 dark:text-zinc-400"`

**Site nav** (TYPO-06) in site-nav.tsx:

1. Nav link sizes are controlled by the NavigationMenuTrigger component which uses `text-sm` (14px) — this already matches TYPO-06 (14px). No change needed there.

2. The locale switcher button (line 117) uses `text-sm` (14px) — correct per TYPO-06. No change.

3. No typography changes needed in site-nav.tsx — all nav text is already at 14px per TYPO-06. Confirm by reading the file that no text sizes need adjustment.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && grep "text-xl font-semibold" src/components/faq-section.tsx && grep "text-\[15px\] font-semibold" src/components/faq-section.tsx && grep "leading-\[1.7\]" src/components/faq-section.tsx && grep "text-xl font-semibold" src/components/tool-cards.tsx && grep "text-\[13px\]" src/components/tool-cards.tsx</automated>
  </verify>
  <acceptance_criteria>
    - faq-section.tsx H2 contains `text-xl font-semibold` (not `text-lg`)
    - faq-section.tsx AccordionTrigger contains `text-[15px] font-semibold`
    - faq-section.tsx AccordionContent contains `text-sm leading-[1.7]`
    - tool-cards.tsx RelatedTools H2 contains `text-xl font-semibold` (not `text-lg`)
    - tool-cards.tsx card description contains `text-[13px]` (not `text-xs`)
    - tool-cards.tsx card title still contains `text-sm font-medium` (14px, unchanged)
    - site-nav.tsx nav trigger text remains `text-sm` (14px per TYPO-06)
  </acceptance_criteria>
  <done>FAQ headings at 20px, FAQ questions at 15px semibold, FAQ answers at 14px with 1.7 line-height; tool card descriptions at 13px; all H2s at 20px; nav links confirmed at 14px</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Visual: H1 headings noticeably larger than before (30px vs 24px)
- Visual: Body/description text slightly larger than before (15px vs 14px)
- Visual: FAQ answers have generous line spacing
</verification>

<success_criteria>
- All H1s use text-3xl (30px) across all 5 tool pages
- All H2s use text-xl (20px) in FAQ and related tools
- Description text uses text-[15px] on all pages
- Textarea content uses text-[15px] font-mono
- FAQ questions: 15px font-semibold; answers: 14px / leading-[1.7]
- Stats: 13px
- Tool card titles: 14px font-medium; descriptions: 13px
- Nav links: 14px (confirmed unchanged)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-foundation/05-02-SUMMARY.md`
</output>
