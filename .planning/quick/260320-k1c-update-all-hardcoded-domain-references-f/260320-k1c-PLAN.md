---
phase: quick
plan: 260320-k1c
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/page.tsx
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/lib/tool-seo.ts
  - messages/en.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "All canonical URLs, hreflang alternates, JSON-LD URLs, sitemap URLs, and robots.txt references use convertcase.uk domain"
    - "No remaining references to textcaseconverter.com in any source file under src/ or messages/"
    - "Vietnamese language label displays correct diacritics"
    - "next build completes with 0 errors"
  artifacts:
    - path: "src/lib/tool-seo.ts"
      provides: "BASE_URL constant used by all tool page metadata and JSON-LD"
      contains: "https://convertcase.uk"
    - path: "src/app/sitemap.ts"
      provides: "Sitemap generation with correct domain"
      contains: "https://convertcase.uk"
    - path: "src/app/robots.ts"
      provides: "Robots.txt with correct sitemap URL"
      contains: "https://convertcase.uk/sitemap.xml"
    - path: "src/app/[locale]/page.tsx"
      provides: "Homepage metadata with correct domain"
      contains: "https://convertcase.uk"
    - path: "messages/en.json"
      contains: "Tiếng Việt"
  key_links: []
---

<objective>
Update all hardcoded domain references from textcaseconverter.com to convertcase.uk and fix Vietnamese language label diacritics in en.json.

Purpose: Domain migration — ensure all SEO-critical URLs (canonical, hreflang, JSON-LD, sitemap, robots) point to the new domain.
Output: Updated source files, passing build.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/tool-seo.ts
@src/app/sitemap.ts
@src/app/robots.ts
@src/app/[locale]/page.tsx
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all domain references and fix translation</name>
  <files>src/lib/tool-seo.ts, src/app/sitemap.ts, src/app/robots.ts, src/app/[locale]/page.tsx, messages/en.json</files>
  <action>
In each of these 4 source files, replace `https://textcaseconverter.com` with `https://convertcase.uk`:

1. `src/lib/tool-seo.ts` line 5: `const BASE_URL = 'https://convertcase.uk'`
2. `src/app/sitemap.ts` line 4: `const BASE_URL = 'https://convertcase.uk'`
3. `src/app/[locale]/page.tsx` line 9: `const BASE_URL = 'https://convertcase.uk'`
4. `src/app/robots.ts` line 9: `sitemap: 'https://convertcase.uk/sitemap.xml',`

Then in `messages/en.json`, change the value at `common.languageSwitcher.vi` from `"Tieng Viet"` to `"Tiếng Việt"`.

After all edits, verify no remaining references to textcaseconverter.com exist in src/ or messages/ by searching with grep.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && ! grep -r "textcaseconverter\.com" src/ messages/ && grep -q "Tiếng Việt" messages/en.json && echo "ALL REPLACEMENTS VERIFIED"</automated>
  </verify>
  <done>Zero occurrences of textcaseconverter.com in source files. Vietnamese label has correct diacritics.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and commit</name>
  <files>N/A</files>
  <action>
Run `next build` from the project root to confirm 0 errors with the new domain. Read the relevant Next.js 16 docs in node_modules/next/dist/docs/ if any build issues arise.

If build succeeds, commit all 5 changed files with message: `chore: update domain to convertcase.uk, fix VI language label`
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>next build exits 0. All changes committed.</done>
</task>

</tasks>

<verification>
- `grep -r "textcaseconverter.com" src/ messages/` returns no results
- `grep "convertcase.uk" src/lib/tool-seo.ts src/app/sitemap.ts src/app/robots.ts src/app/[locale]/page.tsx` returns 4 matches
- `grep "Tiếng Việt" messages/en.json` returns 1 match
- `npx next build` exits 0
</verification>

<success_criteria>
All domain references updated to convertcase.uk, Vietnamese label fixed, build passes, changes committed.
</success_criteria>

<output>
After completion, create `.planning/quick/260320-k1c-update-all-hardcoded-domain-references-f/260320-k1c-SUMMARY.md`
</output>
