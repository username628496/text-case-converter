---
phase: 04-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".planning/phases/04-launch-readiness/04-01-lighthouse-homepage.json", ".planning/phases/04-launch-readiness/04-01-lighthouse-homepage.html", ".planning/phases/04-launch-readiness/04-01-lighthouse-subtool.json", ".planning/phases/04-launch-readiness/04-01-lighthouse-subtool.html", ".planning/phases/04-launch-readiness/04-01-audit-results.md"]
autonomous: true
requirements: ["LAUNCH-VERIFY"]

must_haves:
  truths:
    - "Lighthouse CWV scores for homepage and one sub-tool page are measured and recorded"
    - "Hreflang tags on homepage and one sub-tool (both EN and VI) are verified as bidirectional"
    - "Production URLs for / and /vi/ return HTTP 200 with correct locale content"
    - "Sitemap.xml and robots.txt are accessible on production"
  artifacts:
    - path: ".planning/phases/04-launch-readiness/04-01-audit-results.md"
      provides: "Consolidated audit results with pass/fail per gate"
      contains: "## Audit Results"
    - path: ".planning/phases/04-launch-readiness/04-01-lighthouse-homepage.json"
      provides: "Lighthouse JSON report for homepage"
  key_links:
    - from: "Lighthouse CLI"
      to: "https://text-case-converter-chi.vercel.app/"
      via: "npx lighthouse --output=json --output=html"
      pattern: "lighthouse.*vercel.app"
---

<objective>
Run all automated pre-launch quality gates against the live Vercel production deployment: Lighthouse Core Web Vitals audit, hreflang bidirectional tag verification, production locale routing checks, and sitemap/robots accessibility. Produce a consolidated audit report with pass/fail per gate and specific scores.

Purpose: Determine whether the site meets launch quality thresholds (LCP < 2.5s, CLS < 0.1, INP < 200ms) and SEO correctness before declaring launch-ready. Any failures feed into Plan 02 for fixes.
Output: Lighthouse JSON/HTML reports + audit-results.md with pass/fail summary
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lighthouse CWV audit on homepage and sub-tool</name>
  <read_first>
    - .planning/phases/04-launch-readiness/04-CONTEXT.md (audit strategy and thresholds)
  </read_first>
  <files>.planning/phases/04-launch-readiness/04-01-lighthouse-homepage.json, .planning/phases/04-launch-readiness/04-01-lighthouse-homepage.html, .planning/phases/04-launch-readiness/04-01-lighthouse-subtool.json, .planning/phases/04-launch-readiness/04-01-lighthouse-subtool.html</files>
  <action>
Run Lighthouse CLI against the live Vercel production URL for two pages:

1. Homepage (EN): `npx lighthouse https://text-case-converter-chi.vercel.app/ --output=json --output=html --output-path=.planning/phases/04-launch-readiness/04-01-lighthouse-homepage --chrome-flags="--headless=new" --only-categories=performance`

2. Sub-tool (Password Generator EN): `npx lighthouse https://text-case-converter-chi.vercel.app/password-generator/ --output=json --output=html --output-path=.planning/phases/04-launch-readiness/04-01-lighthouse-subtool --chrome-flags="--headless=new" --only-categories=performance`

After each run, extract these values from the JSON output:
- `audits.largest-contentful-paint.numericValue` (threshold: < 2500ms)
- `audits.cumulative-layout-shift.numericValue` (threshold: < 0.1)
- `audits.interaction-to-next-paint.numericValue` (threshold: < 200ms, may be N/A for static pages)
- `categories.performance.score` (multiply by 100 for percentage)

Record each metric value and whether it passes the threshold. If INP is not reported (common for pages with no user interaction during audit), note it as "N/A — no interaction measured" which is acceptable for static SSG pages.

Note: If `npx lighthouse` is not available, install it first with `npm install -g lighthouse`. Use `--chrome-flags="--headless=new"` for headless Chrome execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-launch-readiness/04-01-lighthouse-homepage.report.json && test -f .planning/phases/04-launch-readiness/04-01-lighthouse-subtool.report.json && echo "Lighthouse reports exist"</automated>
  </verify>
  <acceptance_criteria>
    - File 04-01-lighthouse-homepage.report.json exists and contains "lighthouseVersion"
    - File 04-01-lighthouse-subtool.report.json exists and contains "lighthouseVersion"
    - File 04-01-lighthouse-homepage.report.html exists (non-empty)
    - File 04-01-lighthouse-subtool.report.html exists (non-empty)
  </acceptance_criteria>
  <done>Lighthouse JSON and HTML reports generated for both homepage and password-generator sub-tool page, with CWV metric values extracted</done>
</task>

<task type="auto">
  <name>Task 2: Hreflang verification + locale routing + sitemap/robots checks</name>
  <read_first>
    - .planning/phases/04-launch-readiness/04-CONTEXT.md (verification strategy)
    - src/lib/tool-seo.ts (BASE_URL = https://textcaseconverter.com — canonical domain differs from Vercel URL)
  </read_first>
  <files>.planning/phases/04-launch-readiness/04-01-audit-results.md</files>
  <action>
Run the following automated checks against the live Vercel URL `https://text-case-converter-chi.vercel.app/` and produce a consolidated audit-results.md report.

**A. Hreflang Verification (4 curl calls):**

The hreflang tags reference the canonical domain `https://textcaseconverter.com` (not the Vercel URL). Verify the tags exist and are bidirectional:

1. EN homepage: `curl -sL https://text-case-converter-chi.vercel.app/ | grep -i 'hreflang'`
   - MUST contain: `hreflang="en"` pointing to `https://textcaseconverter.com/`
   - MUST contain: `hreflang="vi"` pointing to `https://textcaseconverter.com/vi/`

2. VI homepage: `curl -sL https://text-case-converter-chi.vercel.app/vi/ | grep -i 'hreflang'`
   - MUST contain: `hreflang="en"` pointing to `https://textcaseconverter.com/`
   - MUST contain: `hreflang="vi"` pointing to `https://textcaseconverter.com/vi/`

3. EN password-generator: `curl -sL https://text-case-converter-chi.vercel.app/password-generator/ | grep -i 'hreflang'`
   - MUST contain: `hreflang="en"` pointing to `https://textcaseconverter.com/password-generator/`
   - MUST contain: `hreflang="vi"` pointing to `https://textcaseconverter.com/vi/password-generator/`

4. VI password-generator: `curl -sL https://text-case-converter-chi.vercel.app/vi/password-generator/ | grep -i 'hreflang'`
   - MUST contain: `hreflang="en"` pointing to `https://textcaseconverter.com/password-generator/`
   - MUST contain: `hreflang="vi"` pointing to `https://textcaseconverter.com/vi/password-generator/`

Verify: EN page references VI, VI page references EN, no self-referential mismatches (both pages reference both locales).

**B. Production Locale Routing (2 curl calls):**

1. `curl -sL -o /dev/null -w "%{http_code}" https://text-case-converter-chi.vercel.app/` — MUST return 200
2. `curl -sL https://text-case-converter-chi.vercel.app/ | grep -i '<title>'` — MUST contain English title text (e.g., "Text Case Converter")
3. `curl -sL -o /dev/null -w "%{http_code}" https://text-case-converter-chi.vercel.app/vi/` — MUST return 200
4. `curl -sL https://text-case-converter-chi.vercel.app/vi/ | grep -i '<title>'` — MUST contain Vietnamese title text

**C. Sitemap and Robots (2 curl calls):**

1. `curl -sL -o /dev/null -w "%{http_code}" https://text-case-converter-chi.vercel.app/sitemap.xml` — MUST return 200
2. `curl -sL https://text-case-converter-chi.vercel.app/sitemap.xml | grep -c 'xhtml:link'` — MUST be > 0 (hreflang entries exist)
3. `curl -sL -o /dev/null -w "%{http_code}" https://text-case-converter-chi.vercel.app/robots.txt` — MUST return 200
4. `curl -sL https://text-case-converter-chi.vercel.app/robots.txt | grep -i 'sitemap'` — MUST reference sitemap URL

**D. Consolidate Results:**

Read the Lighthouse reports from Task 1 (the JSON files in the same directory). Extract CWV scores.

Write `.planning/phases/04-launch-readiness/04-01-audit-results.md` with this structure:

```markdown
# Phase 04 Audit Results

## Core Web Vitals (Lighthouse)

| Page | LCP | CLS | INP | Perf Score | Pass? |
|------|-----|-----|-----|------------|-------|
| Homepage (EN) | {value}ms | {value} | {value}ms | {score}% | {YES/NO} |
| /password-generator/ (EN) | {value}ms | {value} | {value}ms | {score}% | {YES/NO} |

Thresholds: LCP < 2500ms, CLS < 0.1, INP < 200ms (or N/A)

## Hreflang Verification

| Page | EN→VI | VI→EN | Bidirectional | Pass? |
|------|-------|-------|---------------|-------|
| Homepage | {url} | {url} | {YES/NO} | {YES/NO} |
| /password-generator/ | {url} | {url} | {YES/NO} | {YES/NO} |

## Production Locale Routing

| URL | HTTP Status | Correct Content | Pass? |
|-----|-------------|-----------------|-------|
| / | {code} | {YES/NO} | {YES/NO} |
| /vi/ | {code} | {YES/NO} | {YES/NO} |

## Sitemap & Robots

| Resource | HTTP Status | Valid Content | Pass? |
|----------|-------------|---------------|-------|
| /sitemap.xml | {code} | {hreflang count} entries | {YES/NO} |
| /robots.txt | {code} | Sitemap referenced | {YES/NO} |

## Overall

- Total gates: {N}
- Passed: {N}
- Failed: {N}
- Action needed: {YES/NO — list specific failures}
```
  </action>
  <verify>
    <automated>test -f .planning/phases/04-launch-readiness/04-01-audit-results.md && grep -q "## Overall" .planning/phases/04-launch-readiness/04-01-audit-results.md && echo "Audit results complete"</automated>
  </verify>
  <acceptance_criteria>
    - File 04-01-audit-results.md exists
    - File contains "## Core Web Vitals" section with LCP, CLS, INP values for both pages
    - File contains "## Hreflang Verification" section with bidirectional check results
    - File contains "## Production Locale Routing" section with HTTP status codes
    - File contains "## Sitemap & Robots" section with accessibility results
    - File contains "## Overall" section with total gates passed/failed count
  </acceptance_criteria>
  <done>Consolidated audit report exists with pass/fail for all 4 quality gate categories: CWV, hreflang, locale routing, sitemap/robots</done>
</task>

</tasks>

<verification>
- Lighthouse JSON reports exist and contain valid performance data
- Audit results markdown has all 4 sections populated with actual values
- Each gate has a clear PASS or FAIL status
</verification>

<success_criteria>
All automated quality gates have been measured, scores recorded, and pass/fail determined. The audit-results.md file provides a clear go/no-go signal for launch readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-readiness/04-01-SUMMARY.md`
</output>
