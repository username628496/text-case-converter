---
phase: 03-sub-tools-seo-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-05
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "All 4 sub-tools are accessible at EN and VI URLs with working interactive components"
    - "/sitemap.xml is accessible with hreflang entries for all tool pages"
    - "/robots.txt is accessible with correct directives"
    - "Related tools links on all pages route to working tool pages"
    - "next build completes with all pages statically generated"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete phase by running a build and having the user visually confirm all sub-tool pages.

Purpose: Catch any integration issues between components, metadata, translations, and SEO files before marking phase complete.
Output: Confirmed working build with all sub-tools functional.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sub-tools-seo-infrastructure/03-CONTEXT.md
@.planning/phases/03-sub-tools-seo-infrastructure/03-02-SUMMARY.md
@.planning/phases/03-sub-tools-seo-infrastructure/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and verify static generation</name>
  <files>N/A — build verification only, no files created or modified</files>
  <read_first>
    - src/app/[locale]/[tool]/page.tsx (verify TOOL_COMPONENTS has all 4 entries)
    - src/app/sitemap.ts (verify it exists)
    - src/app/robots.ts (verify it exists)
  </read_first>
  <action>
Run `npx next build` and verify:
1. Build completes without errors
2. Static pages are generated for all tool URLs:
   - /reverse-text/ and /vi/reverse-text/
   - /base64-encode-decode/ and /vi/base64-encode-decode/
   - /slug-generator/ and /vi/slug-generator/
   - /password-generator/ and /vi/password-generator/
3. sitemap.xml route is generated
4. robots.txt route is generated

Also run `npx vitest run` to confirm all tests still pass.

If any errors, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/peter/text-case-converter && npx next build 2>&1 | grep -E "(Route|Error|✓|✗)" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits 0
    - Build output shows /reverse-text, /base64-encode-decode, /slug-generator, /password-generator routes
    - Build output shows /sitemap.xml route
    - Build output shows /robots.txt route
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Full build succeeds with all routes statically generated and all tests passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all sub-tools and SEO files</name>
  <files></files>
  <action>
Start the dev server with `npx next dev` and present the verification checklist to the user. User manually tests each tool page for correct interactive behavior, Vietnamese locale support, sitemap/robots output, and related tools navigation.
  </action>
  <what-built>All 4 sub-tool pages (Reverse Text, Base64 Encode/Decode, Slug Generator, Password Generator) with interactive components, metadata, JSON-LD, FAQ sections, related tools navigation, plus sitemap.xml and robots.txt.</what-built>
  <how-to-verify>
    Run `npx next dev` and check each tool:

    1. Visit http://localhost:3000/reverse-text/
       - Type "Hello World" — output should show "dlroW olleH" instantly
       - Click Copy — should see "Copied!" toast
       - Click Clear — input should empty
       - Scroll down — FAQ section with 4+ items visible
       - Related Tools section shows links to other tools

    2. Visit http://localhost:3000/base64-encode-decode/
       - In Encode tab: type "Hello" — output shows "SGVsbG8="
       - Switch to Decode tab: paste "SGVsbG8=" — output shows "Hello"
       - In Decode tab: type "!!!" — should see "Invalid Base64 input" error message

    3. Visit http://localhost:3000/slug-generator/
       - Type "Hello World" — output shows "hello-world"
       - Type "Cà phê Việt Nam" — output shows "ca-phe-viet-nam"

    4. Visit http://localhost:3000/password-generator/
       - Page loads with a generated password (not empty)
       - Drag length slider — password length changes
       - Toggle off "Numbers" — password should have no digits
       - Click Regenerate — new password appears

    5. Visit http://localhost:3000/vi/reverse-text/ — UI is in Vietnamese
    6. Visit http://localhost:3000/sitemap.xml — contains all tool URLs with hreflang
    7. Visit http://localhost:3000/robots.txt — contains sitemap reference

    8. Click Related Tools links on any page — should navigate to working tool pages (no dead links)
  </how-to-verify>
  <verify>User confirms all tools work or reports issues</verify>
  <acceptance_criteria>
    - User types "approved" confirming all 8 verification steps pass
  </acceptance_criteria>
  <done>User has approved all sub-tool pages, sitemap, robots, and related tools navigation</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds with all routes
- All 4 tools interactive and functional
- Vietnamese locale works for all tools
- sitemap.xml and robots.txt accessible
- Related tools links all work (no dead links)
</verification>

<success_criteria>
- User confirms all 4 sub-tools work correctly with the expected UX
- sitemap.xml and robots.txt verified
- No dead links in related tools navigation
</success_criteria>

<output>
After completion, create `.planning/phases/03-sub-tools-seo-infrastructure/03-04-SUMMARY.md`
</output>
